# furadx.github.io

基于 Hexo + NexT 的个人博客源码。

- 源码托管在本仓库，`main` 分支为唯一真源。
- 由 GitHub Actions 自动构建并部署到 GitHub Pages。
- 本地无需安装任何依赖：在 `source/_posts/` 增删 Markdown 并提交即可，CI 自动重建。

站点地址：https://furadx.github.io

## 本地预览（可选）

```bash
npm install
npx hexo server
```
