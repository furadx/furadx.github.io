# NexT 8 主题配置写在这里，不要改 node_modules 里的文件
scheme: Muse              # 单栏居中、顶部菜单、去掉常驻侧栏，最接近 Cactus 的极简观感
menu:
  home: / || fa fa-home
  archives: /archives/ || fa fa-archive
  tags: /tags/ || fa fa-tags
  categories: /categories/ || fa fa-th
darkmode: true

# 侧栏头像与名字（名字取自站点 author: furadx）
avatar:
  url: /images/avatar.png
  rounded: true          # 圆形头像
  rotated: false

# 侧栏社交链接
social:
  GitHub: https://github.com/furadx || fab fa-github

# 侧栏显示：always 让首页等所有页面都能唤出侧栏（Muse 下为可切换面板）
sidebar:
  position: left
  display: always

# 平滑翻页：开启 PJAX，切换页面时局部替换内容、不整页刷新、不闪白
# （motion 默认已开，负责元素渐入动画，与 PJAX 配合就是 Muse demo 的过渡效果）
pjax: true

# 借鉴 Cactus：注入自定义变量与样式（文件位于 source/_data/ 下）
# variable 在 NexT base 变量之后、_colors 之前导入，可覆盖配色变量
# style 在所有样式最后导入，优先级最高
custom_file_path:
  variable: source/_data/variables.styl
  style: source/_data/styles.styl
